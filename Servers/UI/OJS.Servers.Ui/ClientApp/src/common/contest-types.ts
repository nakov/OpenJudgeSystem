/* eslint-disable import/prefer-default-export */

enum ContestType {
    Practice = 0,
    Compete = 1,
}

enum ContestState {
    Active = 'active',
    Past = 'past',
}

export {
    ContestType,
    ContestState,
};
