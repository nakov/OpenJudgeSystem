/* eslint-disable import/prefer-default-export */

enum ContestType {
    Practice = 0,
    Compete = 1,
}

enum ContestState {
    Active = 1,
    Past = 2,
}

export {
    ContestType,
    ContestState,
};
