enum FilterColumnTypeEnum {
    STRING = 'string',
    BOOL = 'boolean',
    INT = 'integer',
    DATE = 'date'
}

export {
    // eslint-disable-next-line import/prefer-default-export
    FilterColumnTypeEnum,
};
